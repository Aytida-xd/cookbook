[project]
name = "atoms-call-control"
version = "0.1.0"
description = "Agent with call control - end calls and transfer to humans"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "smallestai>=4.2.0",
    "python-dotenv",
    "loguru",
]
